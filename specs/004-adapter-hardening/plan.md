---
Summary: Implementation plan for bounding backup retention, adding a content-based composition check, and wiring a skip-aware lint group into the Claude Code adapter.
Tags: [#plan #adapter #hardening]
---

# Implementation Plan: Adapter Hardening

**Branch**: `004-adapter-hardening` | **Date**: 2026-07-25 | **Spec**: [[spec]] — `specs/004-adapter-hardening/spec.md`

**Input**: Feature specification from `specs/004-adapter-hardening/spec.md`

## Summary

Three changes to the live adapter: prune backups to a declared limit, verify the composed output carries
the source content rather than only valid structure, and add a lint group that skips loudly when the
linter is absent. Nothing on the success path may change.

## Technical Context

**Language/Version**: POSIX shell, as the existing adapter

**Primary Dependencies**: none new. `shellcheck` is optional by design — the lint group must work without it.

**Storage**: files; backups beside the target

**Testing**: TDD applies — the deliverable is executable. Three new fixture groups: `retention`,
`composition`, `lint`.

**Target Platform**: the operator's machine

**Project Type**: adapter hardening

**Performance Goals**: none. Pruning touches a handful of files.

**Constraints**: FR-005 is the hard one — byte-identical output on the success path. Every change is
either a new guard on a failure path or housekeeping after a successful write.

**Scale/Scope**: one script, one declaration field, three test groups. No new files except fixtures.

## Constitution Check

| Principle | Gate | Status |
|---|---|---|
| I. Single Source of Truth | No projection hand-edited | PASS — the adapter is touched, the projection is regenerated by it |
| II. Multi-Tool Portability | Vendor values in the declaration | PASS — the retention limit is declared, not hardcoded (FR-001) |
| III. Language Duality | English artifacts | PASS |
| IV. Token Density | N/A to code | PASS |
| V. Spec-Driven Change | Full lifecycle | PASS — analyze gates implementation |
| VI. Review Chain | Loop, round limit 3, check gate | PASS — governs this PR |
| VII. Versioned Instruction Surface | Bump declared | PASS — **N/A**: no instruction content is edited. The adapter is not instruction content |
| VIII. Executable Agent Provisioning | Catalog agreement | PASS — no catalog change |
| IX. Delimited Managed Region | Markers, global-only, protocol | PASS — the update protocol gains a content check; nothing about markers changes |
| X. Capability Tiers | Tier vocabulary | PASS — untouched |
| XI. Isolated Parallel Execution | Worktree, merge order, PR granularity | PASS — see below |
| XII. Operator-Facing README | Claims directive-backed | PASS — the README describes the adapter's behavior, not its retention policy; no operator-visible benefit changes, and the reviewed no-change decision is recorded |
| Authoring constraints | Declaration completeness, idempotency | PASS — one field added |

**Instruction Version Bump**: N/A. No instruction content is edited.

**Parallel Execution Plan** (Principle XI)

- Eligible for parallel dispatch: none. Every implementation task edits `project.sh` or `run-tests.sh`.
- PR granularity: one pull request. The three stories describe one script's behavior and cannot merge
  independently — the Principle XI exception (sanctioned at v1.15.0; constitution is now v1.16.0).

## Design Decisions

**Retention limit: 3, declared.** Enough to recover from a bad projection and the one before it, few
enough that the directory does not grow. Declared in `adapter.yml` so a different operator can choose
differently without editing the script — Principle II.

**Pruning runs AFTER a successful write, never before.** Pruning first would delete the safety net the
write might need. This is the canonical statement of the rationale FR-003 encodes; the tasks reference
FR-003 rather than restating it, so the reasoning lives in one place and cannot drift across four copies.

**The composition check compares a fingerprint, not a line count.** An earlier draft of this plan
proposed a line-count floor and admitted it was weak. That was the wrong resolution: FR-004 demands
rejection on content grounds, and a line count is itself a structural proxy — it would accept a
same-length substitution. The check extracts the region from the composed file and compares its `cksum`
against the `cksum` of the same `$SRC_CONTENT` the composition was built from. Exact by construction, and
the trailing-newline concern that motivated the line-count idea does not arise when both sides come from
the same variable.

**The lint group's third state is the point.** Pass, fail, and SKIPPED — with skipped excluded from the
pass count. A group that reports green because its tool is missing is the `stat -f` defect again: an
assertion that verifies nothing while looking like it did.

## Project Structure

```text
.specify/adapters/claude-code/
├── adapter.yml          # + backup.retain
├── project.sh           # + prune, + composition check
└── tests/run-tests.sh   # + retention, composition, lint groups
.specify/workflows/runs/004-adapter-hardening.md
```

**Structure Decision**: no new files. The adapter directory layout established by feature 003 already
holds everything this feature touches.

## Complexity Tracking

No violations.
